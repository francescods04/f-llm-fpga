# Smoke Run - 2026-05-15

Purpose: verify the first software reference path.

Environment:

```text
PyTorch: 2.9.1
CUDA available: false
MPS available: false
Backend used: CPU
```

Commands:

```bash
PYTHONPATH=src python3 scripts/smoke_model.py

PYTHONPATH=src python3 scripts/train_tiny.py \
  --steps 5 \
  --eval-every 5 \
  --eval-iters 1 \
  --batch-size 2 \
  --seq-len 32 \
  --hidden-size 64 \
  --num-layers 1 \
  --num-heads 4 \
  --local-window 16 \
  --generate-tokens 24

PYTHONPATH=src python3 benchmarks/benchmark_decode.py \
  --checkpoint checkpoints/tiny/model.pt \
  --new-tokens 16 \
  --warmup-tokens 2 \
  --json-out benchmarks/last_decode.json
```

Results:

```text
smoke_model parameters: 795,264
tiny train parameters: 100,928
train tokens: 1,497
val tokens: 167
step 1 train_loss: 5.6528
step 1 val_loss: 5.7701
step 5 train_loss: 5.7329
step 5 val_loss: 5.6807
decode backend: CPU
decode tokens/s: 3112.16
decode ms/token: 0.3213
```

Notes:

- Generated text is not meaningful yet; this run only validates plumbing.
- The local sample corpus is tiny, so the loss values are not research results.
- Next useful step: add a real dataset/tokenizer path and compressed global context.

